# Agent Tools

Diagnostic and verification scripts accumulated across sessions.

| Tool | Purpose |
|------|---------|
| `smoke-test-pipeline.sh` | Submit a URL to staging and verify the full processing pipeline completes (queue → fetch → extract → store → ready). The ONE test that catches runtime-only bugs. |
